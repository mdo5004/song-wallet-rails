<% if record.errors.any? %>
<div class="field-with-errors" id="error_explanation">
    <h4><%= pluralize(record.errors.count, "error") %> prohibited this task from being saved:</h4>

    <ul>
        <% record.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
        <% end %>
    </ul>
</div>
<% end %>